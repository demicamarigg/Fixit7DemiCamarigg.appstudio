/*
function Calculate (num1, num2) {
let sum = num1 + num2
return sum 
}

let X = parseInt(prompt("Enter one number to add"))
let Y = parseInt(prompt("Enter another number to add"))

let summedNumbers= Calculate(X,Y)

alert(`The sum of ${X} and ${Y} is ${summedNumbers}`)

*/